const CodeMinification = () => {
  return (
    <section className="section code-minification">
      <h1>Code Minification</h1>
      <p>
        Code minification is the process of removing all unnecessary characters
        from the source code without changing its functionality. This includes
        removing whitespace characters, comments, and sometimes renaming
        variables to shorter names.
      </p>
    </section>
  );
};

export default CodeMinification;
